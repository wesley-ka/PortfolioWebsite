# /api/* is handled by functions/api/[[path]].js (reads API_ORIGIN env var)

# Voting sub-routes (vote/:id, results/:id) → voting SPA
/vote/*     /voting  200
/results/*  /voting  200

# Greeting card routes (rewrite to root SPA)
/connect    /        200
/hi         /        200
/hello      /        200


